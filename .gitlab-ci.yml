stages:
  - main
  - staging
  - test

build-main:
    stage: main
    tags:
        - rent
    script:
        - rsync -tar --exclude='.git*' --exclude='readme.md'  --stats --no-perms . /var/deploy/${CI_COMMIT_BRANCH}/${CI_COMMIT_SHORT_SHA}


